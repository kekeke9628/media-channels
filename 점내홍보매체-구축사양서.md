# 여주 프리미엄 아울렛 · 점내 홍보매체 관리 시스템 — 구축 사양서

> 확정된 프로토타입(`instore-media-prototype.jsx`)과 기존 가로등 배너 시스템(`kekeke9628/banner-admin`)을 기준으로 새 시스템을 구축한다. 배너 시스템은 **코드 재활용 대상**이며 데이터베이스와 저장소는 완전히 분리한다.

- 저장소: `instore-media-admin`
- 배포: GitHub Pages (`vite base: '/instore-media-admin/'`)
- Supabase: **신규 프로젝트** (배너 시스템과 공유하지 않음)

---

## 1. 목표와 범위

점내 홍보매체에 **어떤 게시물이 어디에 붙어 있고, 언제 빠져야 하는지**를 관리하는 내부 직원용 시스템이다. 수익 관리 도구가 아니므로 단가·계약금액·매출·가동률 개념은 존재하지 않는다. 승인 워크플로도 없다.

해결할 문제는 세 가지다.

1. **위치 파악** — 어느 지점의 어느 매체에 무엇이 붙어 있는가
2. **철거 관리** — 언제 빠져야 하는가, 그리고 **실제로 빠졌는가**
3. **이력 축적** — 과거에 무엇이 언제부터 언제까지 붙어 있었는가

### 전제: 지류 매체이고, 매체는 비지 않는다

**하나. 매체가 비어 있는 경우는 사실상 없다.** 그래서 이 시스템은 *기간이 끝났는데 아직 걸려 있는 것을 찾는 도구*다.

**둘. 지류(인쇄물) 매체다.** 새 것을 걸려면 이전 것을 물리적으로 반드시 떼야 한다. 같은 매체에 기간이 겹치는 게시물은 존재할 수 없고, 후속 게시물이 시작됐다면 이전 것은 반드시 철거된 것이므로 기록이 비어 있으면 시스템이 자동으로 채운다(4.2 참조). 결과적으로 **사람이 조치해야 하는 지연은 "만료" 한 종류뿐이다.**

**셋. 일부 매체는 후속이 확정되지 않는다.** 디렉토리처럼 브랜드가 바뀌지 않는 한 교체할 일이 없는 매체는 종료일을 정할 수 없다. "상시"가 아니라 "시점 미정"이며, 상태명은 **미정**이다(4.1 참조).

**넷. 이력에 구멍이 있으면 그건 없었던 일로 취급한다.** 지류 매체는 비지 않으므로 이론적으로는 게시 공백이 이상 신호지만, 이 시스템은 그것을 별도로 추적하거나 알리지 않는다. 기록되지 않은 기간은 그냥 없는 기간으로 본다.

### 개인정보 최소화

담당자·제작처 등 사람과 관련된 정보는 이 시스템에서 관리하지 않는다. 게시물에 남기는 정보는 **업체명(브랜드)**과 **내용(선택, 비워두면 업체명과 동일)** 뿐이며, 이 원칙은 데이터 모델·화면·알람 전 영역에 동일하게 적용한다.

사용자는 **인증된 내부 직원 한 종류**뿐이다. 공개 화면은 없다. 모든 데이터는 로그인해야 보인다.

기준 시간대는 **Asia/Seoul(KST)**로 통일한다. 모든 날짜는 시분 없는 `date` 타입이며, 오늘 날짜는 배너 시스템의 `getToday()` 구현을 그대로 쓴다.

```js
export const getToday = () =>
  new Intl.DateTimeFormat('sv-SE', { timeZone: 'Asia/Seoul' }).format(new Date());
```

---

## 2. 배너 시스템 대비 — 재사용 / 제거 / 신규

이 절이 이 문서에서 가장 중요하다. 참조 코드를 그대로 복사하면 안 되는 부분이 있다.

### 2.1 그대로 재사용

| 대상 | 비고 |
|---|---|
| `src/constants.js`의 `DAY`, `getToday`, `iso`, `days`, `clamp`, `md` | 그대로 |
| `MapCard.jsx` 성능 구조 | pan을 state 대신 `useRef` + 직접 DOM 조작, 휠 줌은 `useEffect`+ref로 stale closure 방지 — **반드시 유지** |
| `Pin.jsx` 구조 | `React.memo`, `moved` 플래그로 클릭/드래그 구분, `pointerCapture` 드래그 |
| `panToPin` | 하단 목록 클릭 시 지도 이동 |
| 줌 1.5+ 라벨 자동 표시, 선택 핀 글로우 CSS | 그대로 |
| Supabase 매직링크 인증 + `admins` 테이블 + `is_admin()` | 그대로 |
| GitHub Actions cron → Slack Incoming Webhook | 그대로 (시각·조건만 변경) |
| Supabase Storage | 그대로 (버킷명만 변경) |
| Vite + GitHub Pages 배포 워크플로 | `base` 경로만 변경 |

### 2.2 제거

| 대상 | 이유 |
|---|---|
| `applications` 테이블 및 공개 신청 폼 전체 | 외부 신청자가 없다 |
| 공개/관리자 RLS 분리, `banner_availability` 뷰 | 공개 화면이 없다 |
| `UNIT`, `amountOf`, `won`, `TYPE_LABEL`, `unit_price` | 비용 개념이 없다 |
| `STATUS`의 `pending`, `partial`, `avail`, `unavail` | 승인·부분가용 개념이 없다 |
| 승인/반려 큐, `ApprovalConflictModal` | 승인 절차가 없다 |
| Google Sheets 신청서 미러링 (DB 트리거 + pg_net) | 미러링할 신청서가 없다 |
| `DateGate` 기간 조회 팝업 | 가용성 조회가 목적이 아니다 |

### 2.3 신규

| 대상 | 설명 |
|---|---|
| **지점(spots) 레이어** | 배너는 1핀 = 1배너였으나, 점내는 한 지점에 매체가 여러 개다(기둥 4면, 엘리베이터 홀의 랩핑+프레임). 핀은 지점 단위이고 매체는 그 아래 매달린다. |
| **`removed_at`(실제 철거일)** | 예정일과 별개로 실제 철거를 기록한다. 이 시스템의 핵심 필드다. |
| **`overdue`(철거 지연) 파생 상태** | 예정일이 지났는데 `removed_at`이 없는 상태. 매일 Slack 재알림 대상. |
| **이미지 2단 경량화** | 클라이언트 canvas에서 WebP 변환. 원본은 업로드하지 않는다. |
| **`origin_url`** | 원본(AI/PSD)의 구글드라이브 링크를 텍스트로 보관. |

---

## 3. 데이터 모델

### 3.1 media_types (매체 유형)

관리자가 화면에서 직접 추가·수정(아이콘·규격·면수 포함)·보관할 수 있어야 하므로 테이블로 관리한다.

```sql
create table media_types (
  code         text primary key,
  label        text not null,
  default_spec text,
  faces        int  not null default 1,        -- 면수
  glyph        text not null default '▪',      -- 지도·카드에 쓰는 아이콘(이모지/기호)
  color        text not null,
  movable      boolean not null default false,
  open_ended   boolean not null default false,
  sort_order   int  not null default 0,
  active       boolean not null default true
);
```

초기 5종:

| code | label | faces | movable | open_ended |
|---|---|---|---|---|
| `directory` | 디렉토리 | 1 | — | **✓** |
| `ww_fixed` | 고정형 웨더워리어 | 2 | — | — |
| `ww_mobile` | 이동형 웨더워리어 | 2 | **✓** | — |
| `duratrans` | 듀라트란스 | 1 | — | — |
| `fabric` | 패브릭홀더 | 1 | — | — |

`default_spec`은 임시값이며 실측 후 교체한다(12장). 유형은 삭제 대신 `active = false`로 보관한다.

### 3.2 spots (지점)

> **이 절은 구현되지 않았다.** 별도 `spots` 테이블은 만들지 않고 매체에 흡수했다 —
> 지점이 매체와 1:1로 붙어 다녀서 조인만 늘고 얻는 게 없었다. 지금은 `media.zone`
> (EAST_LOW·EAST_HIGH·WEST_LOW·WEST_MIDDLE·WEST_HIGH 중 하나, CHECK 제약)과
> `media.x`/`media.y`(배치도 좌표)가 그 역할을 한다. 아래 내용은 구역 구분과 좌표
> 개념의 원본 명세로만 읽을 것.

구역은 **EAST LOW / EAST HIGH / WEST LOW / WEST MIDDLE / WEST HIGH** 5개로 고정한다.

```sql
create table spots (
  id         text primary key,
  name       text not null,
  zone       text not null check (zone in ('EAST_LOW','EAST_HIGH','WEST_LOW','WEST_MIDDLE','WEST_HIGH')),
  x          numeric(5,2) not null,
  y          numeric(5,2) not null,
  active     boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

핀 위치는 관리자가 드래그로 옮기고 **저장을 눌러야 커밋**한다(배너 시스템의 draft/save/undo 동작을 그대로 이식). 저장 전에는 서버에 반영하지 않는다.

### 3.3 media (매체)

```sql
create table media (
  id         text primary key,
  type       text not null references media_types(code),
  name       text not null,
  faces      int  not null default 1,     -- 유형 기본값을 불러오되 개별 조정 가능
  spec       text,
  -- 3.2 spots를 흡수한 자리 — 구역과 배치도 좌표를 매체가 직접 갖는다
  zone       text not null check (zone in ('EAST_LOW','EAST_HIGH','WEST_LOW','WEST_MIDDLE','WEST_HIGH')),
  x          numeric not null,
  y          numeric not null,
  active     boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

**`faces`(면 수)는 장소마다 자유롭게 바꿀 수 있어야 한다.** 듀라트란스는 한 자리에 10면이
모여 있는 곳도, 6면인 곳도 있다. 유형 기본값으로 만들되 매체 상세에서 언제든 조정한다 —
줄일 때는 사라질 면에 살아 있는 배치가 있으면 막는다.

**매체 개수는 수시로 변한다.** 관리자 화면에서 유형·지점을 고르고 개수를 지정해 여러 개를 한 번에 등록할 수 있어야 한다.

**게시 이력이 있는 매체는 삭제하지 않는다.** 지우면 과거 기록이 함께 사라지므로 `active = false`로 보관한다. 보관된 매체는 지도·현황에서 빠지고 이력 조회에는 남는다. 이력이 전혀 없는 매체만 완전 삭제를 허용한다.

### 3.4 postings · posting_variants · placements (홍보물과 배치)

> 이 절은 2026-08-24 기준 실제 스키마다. 처음 사양에서는 "게시건과 시안을 한 테이블로
> 합친다"고 잡았지만, 운영에 들어가 보니 두 가지가 어긋났다.
> ① 같은 캠페인(예: "나이키 여름 기획전")을 웨더워리어용·듀라트란스용으로 각각 인쇄하는데,
>    한 테이블에 유형이 하나뿐이라 같은 캠페인을 유형 수만큼 따로 등록해야 했고 서로 묶이지도
>    않았다. 등록할 때 고른 유형을 나중에 고칠 방법도 없어서, 잘못 고르면 그 홍보물은 못 걸었다.
> ② 같은 홍보물을 여러 매체에 거는 일이 흔한데, 게시건과 시안이 붙어 있으면 매체마다 홍보물을
>    새로 만들어야 했다.
> 그래서 **캠페인(postings) → 규격별 인쇄 파일(posting_variants) → 매체별 배치(placements)**
> 세 층으로 나눴다.

```sql
-- 캠페인. 어느 매체에 언제 걸리는지, 어떤 파일을 쓰는지는 이 테이블이 모른다.
create table postings (
  id         uuid primary key default gen_random_uuid(),
  brand      text not null,            -- 업체명 (필수)
  title      text,                     -- 내용 (선택 — 비어 있으면 화면에서 brand로 표시)
  created_by uuid references auth.users(id),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- 규격(매체 유형)별 인쇄 파일. 웨더워리어 900×1800과 듀라트란스 1030×1456은 물리적으로
-- 다른 인쇄물이라 파일이 따로다. 캠페인에 규격을 나중에 얹을 수 있다.
create table posting_variants (
  id          uuid primary key default gen_random_uuid(),
  posting_id  uuid not null references postings(id) on delete cascade,
  type        text not null references media_types(code),
  thumb_path  text,
  view_path   text,                    -- 파일 없이 규격만 먼저 등록해 둘 수 있다
  bytes_orig  int not null default 0,
  bytes_light int not null default 0,
  created_at  timestamptz not null default now(),
  unique (posting_id, type)            -- 한 캠페인에 같은 규격이 두 벌 있을 이유가 없다
);

-- 배치 = "이 캠페인을 이 매체의 이 면에 이 기간 동안 건다". 설치 확인 사진은 캠페인이
-- 아니라 이 배치에 붙는다 — "실제로 이 자리에 걸렸다"는 증빙이기 때문.
create table placements (
  id                 uuid primary key default gen_random_uuid(),
  posting_id         uuid not null references postings(id),
  media_id           text not null references media(id),
  face               smallint not null default 1,   -- 여러 면을 가진 매체에서 몇 번째 면인지(1부터)
  face_label         text,                          -- 직접 붙인 면 이름(비면 화면에서 "N면")
  start_date         date not null,
  end_date           date,                          -- null = 종료일 미정
  removed_at         date,
  removal_source     text check (removal_source in ('manual','auto')),
  install_photo_path text,
  created_at         timestamptz not null default now()
);
```

**어느 규격 파일을 쓰는지는 배치에 기록하지 않는다.** 그 배치가 걸린 매체의 유형이 이미
결정하기 때문이다(`variantFor(posting, media.type)`). 그래서 규격을 나중에 더해도 배치
데이터는 손댈 필요가 없다.

**같은 매체의 같은 면에 기간이 겹치는 배치는 물리적으로 존재할 수 없다.** 애플리케이션
검사에 의존하지 말고 DB 제약으로 강제한다. 처음 사양과 달리 **면(face)까지 포함해서**
겹침을 판정한다 — 듀라트란스 10연동처럼 한 자리에 여러 면이 있는 매체가 실제로 있고,
면마다 광고주가 다른 게 보통이기 때문이다.

```sql
create extension if not exists btree_gist;

alter table placements
  add check (end_date >= start_date),
  add check (face >= 1),
  add exclude using gist (
    media_id with =,
    face with =,
    -- 종료일 미정(null)이면 무한대로 취급해 이후 배치와 겹치지 않게 한다
    daterange(start_date, end_date, '[]') with &&
  );  -- 실제 제약 이름: placements_media_id_face_daterange_excl
```

등록·수정 시 이 제약에 걸리면 어떤 배치와 겹치는지 조회해 사용자에게 알려준다.

**등록 화면의 기본 동작:** 매체를 고르면 그 면의 마지막 배치 종료일 다음 날을 시작일로 자동
채워, 기본값 그대로는 겹치지 않게 한다. 사용자가 날짜를 직접 겹치게 바꾸면 제출 시 "겹치는
홍보물이 있습니다 — 그대로 진행하시겠습니까?"를 보여주고, **진행을 선택하면 기존 배치의
종료일을 새 배치 시작일 하루 전으로 조정한 뒤** 새 배치를 등록한다. 마지막 배치가 종료일
미정인 면도 같은 흐름으로 처리된다.

**담당자·제작처는 이 시스템에서 관리하지 않는다.** 데이터 모델·화면·알람 전체에 동일하게 적용한다.

### 3.5 admins (허용 직원)

```sql
create table admins (
  user_id    uuid primary key references auth.users(id) on delete cascade,
  email      text not null,
  name       text,
  role       text not null default 'editor' check (role in ('editor','viewer')),
  created_at timestamptz not null default now()
);
```

`viewer`는 조회만 가능하고 게시물 등록·철거 기록·핀 이동을 할 수 없다. 초기 계정은 `kim9628@premiumoutlets.co.kr`.

---

## 4. 파생 상태 규칙

상태는 저장하지 않고 **날짜에서 계산한다.** 저장하면 반드시 실제와 어긋난다.

```
removed_at 있음                → removed  (철거 완료)
start_date > 오늘              → upcoming (게시 예정)
end_date 가 null               → open     (미정)      ← 알람 제외
end_date < 오늘                → overdue  (만료)      ← 유일한 조치 대상
그 외                          → live     (게시중)
```

### 4.1 미정 상태 (`open`)

**"상시 게시물"이 아니라 "후속이 확정되지 않은 게시물"이다.** 디렉토리가 대표적이다 — 브랜드가 교체되지 않는 한 맵을 바꿀 일이 없으므로, 철거일을 미리 정할 수 없다. 영원히 두겠다는 뜻이 아니라 **시점을 모른다**는 뜻이다. 상태명은 **미정**이다.

- `end_date`를 `null`로 두어 표현한다.
- **철거 알람 대상이 아니다.**
- 경과일은 계속 센다. `open_days ≥ 365`인 건은 주간 요약에 **장기 미정**으로 집계한다. 개별 알람은 보내지 않는다.
- 실제 교체가 결정되면 종료일을 확정 입력하고, 그 뒤에 후속 게시물을 등록한다.
- `media_types.open_ended = true`인 유형은 게시물 등록 화면에서 **종료일 미정이 기본 선택**된다.

매체 단위 파생값:

- **철거 지연 일수(overdue_days)** = 오늘 − `end_date`
- **철거까지 남은 일수(d_to_remove)** = `end_date` − 오늘
- **비어있음(is_empty)** = `live`도 `overdue`도 없는 상태. 예외 상황이다.
- **비어있는 일수(empty_days)** = 오늘 − 가장 최근 `removed_at`

### 4.2 자동 철거 처리

지류 매체이므로 **후속 게시물이 시작됐다면 이전 게시물은 반드시 철거됐다.** 이 상태에서 `removed_at`이 비어 있는 것은 현장 문제가 아니라 기록 누락이므로, 시스템이 자동으로 채운다. 사람에게 알리지 않는다.

```sql
-- 후속 게시물이 이미 시작된 게시물의 철거 기록을 채운다.
update postings p
set    removed_at = n.start_date,
       removal_source = 'auto',
       updated_at = now()
from   postings n
where  p.removed_at is null
  and  n.media_id = p.media_id
  and  n.start_date > p.start_date
  and  n.start_date <= (now() at time zone 'Asia/Seoul')::date
  and  not exists (              -- 바로 다음 게시물만
         select 1 from postings x
         where x.media_id = p.media_id
           and x.start_date > p.start_date and x.start_date < n.start_date);
```

일 1회 크론에서 알람 조회 **직전에** 실행한다. `removal_source`로 자동/수동을 구분해 남기므로 이력의 신뢰도가 떨어지지 않는다. 화면에서도 자동 처리분은 `자동` 배지로 표시한다.

이 처리 덕분에 **사람이 조치해야 하는 지연은 만료 한 종류뿐**이 된다.

- **만료** = `end_date < 오늘` **+** `removed_at is null` **+** 후속 게시물 없음
- 급하진 않지만 방치되면 지난 시즌 물건이 계속 노출되므로, **구역별로 묶어** 한 번에 정리할 수 있게 보여준다.

### 4.3 이력 공백은 추적하지 않는다

지류 매체는 이론적으로 비지 않으므로 게시 기간 사이에 공백이 있다면 그건 기록 누락일 수 있다. 그러나 이 시스템은 **그것을 별도로 계산하거나 화면·알람에 노출하지 않는다.** 기록되지 않은 기간은 없었던 일로 취급한다. 과거 버전에서 검토했던 이력 공백 뷰·배지·탭은 전부 제거한다.

### 4.4 뷰

> **이 절은 구현되지 않았다.** 아래 두 뷰는 설계 단계의 안이고, 실제 DB에는 만들지 않았다.
> 상태 판정(`live`/`open`/`overdue`/`upcoming`/`removed`)과 면 단위 슬롯 계산은 프런트엔드
> `src/lib/status.js`가 한다 — 화면이 이미 모든 배치를 들고 있어 왕복이 한 번 줄고, 오프라인
> 상태에서도 같은 규칙으로 계산되기 때문. 아래 SQL은 판정 규칙의 원본 명세로만 읽을 것이며,
> 컬럼 구성은 3.4의 현재 스키마와 다르다(postings가 아니라 placements 기준).


```sql
create or replace view v_posting_status as
select p.*,
  case
    when p.removed_at is not null then 'removed'
    when p.start_date > (now() at time zone 'Asia/Seoul')::date then 'upcoming'
    when p.end_date is null then 'open'                                       -- 미정
    when p.end_date < (now() at time zone 'Asia/Seoul')::date then 'overdue'  -- 만료
    else 'live'
  end as status
from postings p;
```

```sql
create or replace view v_media_state as
with t as (select (now() at time zone 'Asia/Seoul')::date as d),
cur  as (select distinct on (media_id) * from v_posting_status
         where status='live'     order by media_id, start_date desc),
opn  as (select distinct on (media_id) * from v_posting_status
         where status='open'     order by media_id, start_date desc),
ovd  as (select distinct on (media_id) * from v_posting_status
         where status='overdue'  order by media_id, end_date  desc),
nxt  as (select distinct on (media_id) * from v_posting_status
         where status='upcoming' order by media_id, start_date asc),
rm   as (select distinct on (media_id) * from v_posting_status
         where removed_at is not null order by media_id, removed_at desc)
select
  m.id, m.spot_id, m.type, m.name,
  s.zone, s.name as spot_name, s.x, s.y,
  cur.id as current_id, cur.title as current_title, cur.end_date as remove_due,
  (cur.end_date - t.d) as d_to_remove,
  opn.id as open_id, opn.title as open_title,
  (t.d - opn.start_date) as open_days,
  ovd.id as overdue_id, ovd.title as overdue_title, ovd.end_date as overdue_due,
  (t.d - ovd.end_date) as overdue_days,
  nxt.id as next_id, nxt.brand as next_brand, nxt.start_date as next_start,
  (cur.id is null and opn.id is null and ovd.id is null) as is_empty,
  case when cur.id is null and opn.id is null and ovd.id is null
       then (t.d - rm.removed_at) end as empty_days
from media m
cross join t
join spots s on s.id = m.spot_id
left join cur on cur.media_id = m.id
left join opn on opn.media_id = m.id
left join ovd on ovd.media_id = m.id
left join nxt on nxt.media_id = m.id
left join rm  on rm.media_id  = m.id
where m.active and s.active;
```

지도·매체 현황·빈 매체·철거 예정 화면과 Slack 크론이 모두 이 뷰 하나를 읽는다.

---

### 4.5 이동형 매체

`media_types.movable = true`인 유형(이동형 웨더워리어)은 **지점이 고정되지 않는다.** 지도 핀에 고정 배치할 수 없으므로 `media.spot_id`를 화면에서 언제든 변경할 수 있게 한다. 매체 상세와 매체 관리 화면에서 지점 드롭다운으로 바꾼다.

**위치 이동 이력을 남길지는 미확정이다(12장).** 남긴다면 별도 테이블이 필요하다.

```sql
-- 위치 이동 이력이 필요할 경우에만 추가
create table media_moves (
  id         uuid primary key default gen_random_uuid(),
  media_id   text not null references media(id),
  from_spot  text references spots(id),
  to_spot    text not null references spots(id),
  moved_at   date not null,
  moved_by   uuid references auth.users(id),
  note       text
);
```

## 5. 권한 (RLS)

배너 시스템과 달리 **공개 접근이 전혀 없다.** `anon` 역할은 모든 테이블에서 차단한다.

```sql
create or replace function is_staff()
returns boolean language sql security definer stable as $$
  select exists (select 1 from admins where user_id = auth.uid())
$$;

create or replace function is_editor()
returns boolean language sql security definer stable as $$
  select exists (select 1 from admins where user_id = auth.uid() and role = 'editor')
$$;
```

| 테이블 | SELECT | INSERT / UPDATE / DELETE |
|---|---|---|
| media_types | `is_staff()` | `is_editor()` |
| spots | `is_staff()` | `is_editor()` |
| media | `is_staff()` | `is_editor()` |
| postings | `is_staff()` | `is_editor()` |
| admins | 본인 행만 | 차단 (Supabase 콘솔에서 직접 추가) |

뷰 두 개는 `authenticated`에만 `grant select` 한다. `anon`에는 어떤 권한도 주지 않는다.

---

## 6. 이미지 처리

Supabase 무료 티어를 오래 쓰기 위해 **원본은 앱에 올리지 않는다.**

### 6.1 업로드 흐름

1. 직원이 이미지 파일을 선택한다.
2. 브라우저 canvas에서 두 벌을 만든다. 서버 비용이 들지 않는다.
   - `view` — 긴 변 1600px, WebP 품질 0.75 (150~300KB)
   - `thumb` — 긴 변 400px, WebP 품질 0.70 (15~30KB)
3. 두 벌만 Storage 버킷 `media-images`에 올린다. 원본은 올리지 않는다.
4. 원본(AI/PSD 등)은 직원이 구글드라이브에 두고 **링크를 `origin_url`에 붙여넣는다.**

프로토타입 `AddModal`의 `process()` 함수가 이 변환의 기준 구현이다. 그대로 이식한다.

### 6.2 규격 검증

`media_types.default_spec`(또는 `media.spec`)에서 가로/세로를 파싱해 비율을 구하고, 업로드 이미지 비율과 **8% 이상 어긋나면 경고**한다. 차단하지는 않는다 — 잘림을 감수하고 올리는 경우가 있다.

### 6.3 버킷과 접근

버킷 `media-images`는 **비공개**로 만들고 서명 URL로 읽는다. 목록 화면은 `thumb`만 로드한다(카드 100장 ≒ 2~3MB). 상세 패널에서만 `view`를 로드한다.

### 6.4 아카이빙

`removed_at`으로부터 **6개월이 지난 게시물은 `view` 파일을 삭제하고 `thumb`만 남긴다.** 이력 열람에는 썸네일로 충분하다. 주 1회 도는 크론이 처리하며 `view_path`를 `null`로 갱신한다.

---

## 7. Slack 알람

매일 **09:00 KST**(cron UTC `0 0 * * *`)에 GitHub Actions가 실행되어 Supabase(service role key)에서 조회 후 Incoming Webhook으로 POST한다. `ALERT_DAYS_BEFORE`는 환경변수이며 기본값 **3**이다.

크론은 **알람 조회 직전에 4.2의 자동 철거 처리를 먼저 실행한다.** 그래야 기록 누락이 만료로 잘못 잡히지 않는다.

| # | 조건 | 주기 | 메시지 |
|---|---|---|---|
| ① 철거 예고 | `remove_due = 오늘 + N`, N=`ALERT_DAYS_BEFORE`(기본 3) | 매일 09:00 | `[매체명] 업체명 (~종료일)` — 설명 문구 없이 대상과 날짜만 전달 |
| ② **만료** | `overdue_id is not null` | **매일, 구역별 묶음 1건** | `만료된 게시물 N건` + 구역별 목록 |
| ③ 주간 요약 | — | 월 09:00 | 게시중/전체, 만료 수, 미정 수(장기 건수 포함) |

**미정(`open`)은 어떤 알람에도 나가지 않는다.** 종료일이 없으므로 ①의 대상이 아니고, 만료로도 잡히지 않는다. 장기 건수만 ③에 집계된다.

①·②는 부가 설명 문구를 넣지 않는다. 매체·업체명·날짜 등 판단에 필요한 값만 전달한다.

**기준일은 관리자가 화면에서 바꿀 수 있다.** 운영 중인 '오늘'을 강제로 앞당기거나 되돌려 알람·상태가 어떻게 나오는지 미리 점검할 수 있어야 한다. 실제 운영에서는 크론이 실행 시각의 KST 날짜를 기준일로 쓰고, 관리자 화면에서의 기준일 변경은 조회·점검용으로 클라이언트에서만 적용한다(서버 데이터를 바꾸지 않는다).

②를 **해소될 때까지 매일 재발송**하는 것이 중요하다. 한 번만 보내면 놓친 건이 그대로 방치되고, 지난 시즌 물건이 계속 노출된다.

다만 건당 개별 발송하면 채널이 시끄러워져 결국 무시된다. **하루 한 건, 구역별로 묶어서** 보낸다.

`service role key`는 절대 클라이언트 번들에 넣지 않는다. GitHub Actions Secrets에만 둔다.

---

## 8. 배치도

배너 시스템과 동일한 방식을 쓴다. 관리자가 배치도 이미지를 올리면 **지도 프레임 비율(가로세로 2:1)로 자동 중앙 크롭**해 배경으로 적용한다. 별도 이미지 편집 도구 없이 업로드만으로 끝난다. 이미지가 없으면 구역 사각형만 그린 임시 배경을 쓴다. 지점 좌표는 %로 저장되어 있으므로 이미지를 나중에 교체해도 핀 위치는 그대로 유지되며, 다만 실제 배치와 어긋나면 핀을 재배치(드래그 후 저장)한다.

---

## 9. 프론트엔드 구성

프로토타입 컴포넌트를 이어받아 목 데이터를 Supabase 쿼리로 교체한다.

화면: **홍보물 관리**(기본) / 매체 현황 / 게시물 / 타임라인 / 매체 관리 / 알람 예정. (구 '철거 관리'는 '홍보물 관리'로 개편·흡수되었고, '만료 방치'·'이력 조회' 탭은 폐지되어 각각 홍보물 관리와 타임라인에 통합되었다.)

**기간 조회는 목록형 화면 전체에 공통 패턴으로 적용한다.** 홍보물 관리 · 매체 현황 · 게시물 · 타임라인 네 화면 모두 "기간으로 조회" 체크박스와 시작일·종료일 입력을 갖는다. 켜면 그 화면은 **현재 상태 스냅샷**이 아니라 **그 기간과 겹치는 모든 게시물의 이력 검색**으로 전환된다 — "몇 월 며칠부터 며칠까지 어떤 매체에 무엇이 걸려 있었는지"를 어느 화면에서 물어도 같은 방식으로 답이 나와야 한다. 키워드 검색(업체명·내용·매체명)은 기간 조회 여부와 무관하게 항상 열려 있다.

- **홍보물 관리** — 모든 홍보물을 **만료일 기준으로 정렬**하고 맨 앞에 만료 건이 오도록 한다. 매체 유형별 필터, 기간 지정 검색, 키워드 검색을 제공한다.
- **매체 현황** — 매체·유형·지점·업체명·내용·상태만 보여준다. 담당자·제작처는 노출하지 않는다. 기간을 지정하면 스냅샷 대신 해당 기간의 게시 이력을 보여준다.
- **매체 관리** — 매체 유형 추가·수정(아이콘·규격·면수)·보관, 매체 일괄 등록(개수·면수 확인), 보관/삭제, 이동형 매체의 지점 변경. (기간 조회 대상이 아니다 — 마스터 데이터 관리 화면이므로 이력 검색과 무관하다.)
- **타임라인** — 과거의 '이력 조회'를 흡수했다. 검색어로 필터링하고, 막대의 업체명을 클릭하면 매체 상세로 이동한다. '표로 보기' 토글로 동일한 데이터를 표 형태로도 볼 수 있다. 기간을 지정하면 그래프의 가로축 범위 자체가 지정 구간으로 바뀐다.
- 지도는 지점 핀. 만료가 있으면 빨강, 아니면 초록, 아무것도 없으면 회색. 배지 숫자는 떼야 할 매체 수. 배치도는 실제 이미지를 올리면 가로등 배너 센터맵과 동일하게 **자동으로 중앙 크롭**되어 배경으로 적용된다.

구역 필터는 현재 지도 패널에만 있고 목록 화면들에는 없다. 필요하면 매체 유형 필터와 같은 방식으로 추가한다(12장 미확정 항목).
- 핀 클릭 → 지점 시트 → 매체 선택 → 매체 상세(현재 게시물 + 게시 이력).
- 철거 완료 기록은 **한 번의 클릭**으로 끝나야 한다. 되돌리기도 제공한다.
- 반응형(모바일 / 태블릿 / PC)을 유지한다. 현장에서 휴대폰으로 볼 가능성이 높다.

---

## 10. 배포

Vite 빌드를 GitHub Pages에 올린다(`base: '/instore-media-admin/'`). 클라이언트에는 Supabase URL과 anon key만 들어가며, 실제 접근 통제는 RLS가 담당한다. GitHub Actions Secrets에는 `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SLACK_WEBHOOK_URL`, `ALERT_DAYS_BEFORE`를 등록한다.

---

## 11. 구축 순서 (마일스톤)

1. Vite + React 스캐폴딩, `base` 설정, 프로토타입 UI 이식
2. Supabase 신규 프로젝트 · 스키마(3장) · 뷰(4장) · 시드 데이터
3. RLS(5장) + 매직링크 인증 + `admins` 판별 + 라우팅 게이팅
4. `v_media_state` 읽기 연동 — 지도·매체 현황을 실데이터로 전환
5. `MapCard` / `Pin` 이식 — 지점 핀 드래그·저장·되돌리기
6. **매체 관리 화면** — 유형 CRUD(보관), 매체 일괄 등록/보관, 이동형 지점 변경
7. 게시물 CRUD + 종료일 미정 처리 + 겹침 배제 제약(`btree_gist`) + 이미지 2단 업로드
8. 철거 완료 기록 / 기록 취소 + **자동 철거 처리 배치**(4.2)
9. 홍보물 관리(필터·기간 검색·키워드 검색) · 타임라인(검색·표 전환) 화면 완성
10. GitHub Actions cron + Slack 알람 3종
11. 아카이빙 크론(6개월 경과 `view` 삭제) + GitHub Pages 배포

---

## 12. 미확정 항목

구축 중 운영자 확인이 필요하다. 확정 전까지는 프로토타입 값으로 진행한다.

1. **매체 유형별 실제 규격** — 5종의 `default_spec`은 임시값이다. 실측값으로 교체해야 시안 규격 검증이 의미를 갖는다.
2. **지점·매체 실제 목록과 수량** — 시드 데이터를 실제 자산 목록으로 교체한다. 유형별 개수는 관리 화면에서 등록 가능하다.
3. **배치도 원본** — 받는 즉시 8장의 교체 절차로 반영한다.
4. **철거 완료 기록 주체** — 담당자가 사무실에서 처리하면 현재 구조로 충분하다. 설치기사가 현장에서 처리한다면 모바일 전용 간이 화면(오늘 철거 목록 → 사진 촬영 → 완료 체크)을 별도로 추가한다.
5. **이동형 매체의 위치 이동 이력** — 현재 위치만 알면 되는지, 언제 어디서 어디로 옮겼는지 이력이 필요한지. 필요하면 4.5의 `media_moves` 테이블을 추가한다.
6. **기존 게시물의 종료일** — 시스템 도입 시점에 이미 걸려 있는 물건들의 철거 예정일을 모를 수 있다. 모르는 채로 종료일을 넣으면 전부 만료로 잡히므로, **모르는 건은 종료일 미정(미정 상태)으로 등록**하고 실사 후 확정하는 방식을 권한다.
7. **장기 미정 기준일** — 현재 365일. 디렉토리가 보통 몇 년 유지되는지에 따라 조정한다.
8. **목록 화면의 구역 필터** — 지금은 지도에만 있다. 홍보물 관리·매체 현황 등에도 구역 필터를 넣을지는 실제 사용 빈도를 보고 결정한다.
